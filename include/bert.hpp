#pragma once
#include <iostream>

class BERT {
private:
    std::string message;
public:
    BERT(); // Declaración del constructor
    // Otras declaraciones de métodos (si los hay)
    void printMessage();

};